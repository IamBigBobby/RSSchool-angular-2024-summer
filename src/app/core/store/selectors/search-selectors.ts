// import { createSelector } from '@ngrx/store';
// import { AppState } from '../state/app-state';
// import { SearchWordState } from '../state/search-word-state';

// export const selectFeature = (state: AppState) => state.searchWord;

// export const selectSearchWord = createSelector(
//   selectFeature,
//   (state: SearchWordState) => state.searchWord,
// );
