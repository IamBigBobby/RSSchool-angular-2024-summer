import { FilteringKeyWordPipe } from './filtering-key-word.pipe';

describe('FilteringKeyWordPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteringKeyWordPipe();
    expect(pipe).toBeTruthy();
  });
});
